// Notacion

// ;  --> delimita el final de una linea de codigo (actualmente es prescindible)

const b = 4;

console.log(b + 4);

// .  --> Se utiliza en los objetos para acceder a sus atributos

//movil.anchura

// []  --> Listas , arreglos o arrays

const ar = [1, 2, 3, 4];
console.log(ar[2]);

// () --> se utilizan para funciones

function suma(a, b) {
  //se escribe la funcion
}

// {}  --> llaves para objetos, funciones y estructuras de funcion

const movil = {
    anchura: 5,
    altura: 10
};

if (true) {
    const constante2 = "Hola";
}

